const Fallback = () => {
  return (
    <div className="flex items-center justify-center w-full h-[500px]">
      <p className="text-gray-500 text-center text-lg font-semibold">
        You haven't added todos' yet 😊
      </p>
    </div>
  );
};

export default Fallback;
